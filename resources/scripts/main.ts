window.onload = function(){
    let eLines = document.querySelectorAll('.js-typewriting');
    for (let i=0; i < eLines.length; i++){
        let ePromptIcon = eLines[i].querySelector('.js-prompt-icon');
        let ePromptText = eLines[i].querySelector('.js-text');
        
    }
}

// function dispChars (e: element , l: Array<string>) {
//    return 
// };
